{
  "name": "cre-workflow-contracts",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Contracts use pre-compiled output/ or run: solc --abi --bin --optimize --base-path . --include-path node_modules/ -o output/ TokenizedAssetPlatform.sol'",
    "lint": "biome check .",
    "deploy": "bun run 1_deploy.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "Tokenized Asset Platform smart contracts",
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.3",
    "js-yaml": "^4.1.1",
    "viem": "^2.44.4"
  },
  "devDependencies": {}
}
